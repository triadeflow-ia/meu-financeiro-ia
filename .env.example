# Variáveis para o build do frontend no Docker (docker-compose build).
# Copie para .env na raiz e preencha; o backend usa backend/.env.
VITE_API_KEY=
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key
